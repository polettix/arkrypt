#!/usr/bin/env perl
use 5.024;
use warnings;
use App::Arkrypt;

my $exval = App::Arkrypt->run(@ARGV) || 0;
exit $exval;
__END__

=pod

=encoding utf-8

=head1 NAME

arkrypt - archive encrypted stuff

=head1 SYNOPSIS

   $ arkrypt l

=head1 DESCRIPTION

More to come

=cut
